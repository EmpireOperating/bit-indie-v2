# Server
HOST=127.0.0.1
PORT=8787

# Database
DATABASE_URL=postgresql://marketplace:marketplace@127.0.0.1:55432/marketplacepostgresql://marketplace:marketplace@127.0.0.1:55432/marketplace?schema=marketplace_api_v2

# Storage (S3 compatible)
S3_ENDPOINT=http://127.0.0.1:59000
S3_REGION=auto
S3_BUCKET=marketplace
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minioadmin
S3_PUBLIC_BASE_URL=http://127.0.0.1:59000/marketplace

# Security
# (placeholder until Embedded Signer integration)
API_JWT_SECRET=dev-change-me

# Payouts (OpenNode)
# If OPENNODE_API_KEY is unset/blank, the payout worker falls back to the mock provider.
OPENNODE_API_KEY=
# Optional override for testing (ex: https://dev-api.opennode.co)
OPENNODE_BASE_URL=
# Where OpenNode should POST withdrawal status updates.
# Example: https://<api-host>/webhooks/opennode/withdrawals
OPENNODE_WITHDRAWAL_CALLBACK_URL=
